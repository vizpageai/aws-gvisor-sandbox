[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gvisor-sandbox"
version = "0.2.0"
description = "Run Python code in Kubernetes pods backed by the gVisor RuntimeClass."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "kubernetes>=30.1.0",
]

[project.scripts]
gvisor-sandbox = "gvisor_sandbox.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
